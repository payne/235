<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title | default("9 PM Friday Technical Ragchew NET") }}</title>
  <meta name="description" content="9 PM Friday Technical Ragchew NET on W0JJK 145.235 MHz in Omaha, Nebraska.">
  <link rel="stylesheet" href="/assets/css/style.css">
</head>
<body>
  <!-- Top Alert Banner -->
  <div class="top-banner" id="topBanner">
    <span class="top-banner-text">Sarpy EMA may preempt this NET any time.</span>
    <button class="top-banner-close" onclick="closeTopBanner()" aria-label="Close banner">&times;</button>
  </div>

  <header class="site-header">
    <div class="header-container">
      <a href="/" class="site-logo">Friday .235 NET at 9 pm</a>
      <button class="menu-toggle" id="menuToggle" aria-label="Toggle menu" aria-expanded="false">
        <span class="hamburger"></span>
      </button>
      <nav class="site-nav" id="siteNav">
        <ul>
          <li><a href="/"{% if page.url == "/" %} class="active"{% endif %}>Home</a></li>
          <li><a href="/flyer/"{% if page.url == "/flyer/" %} class="active"{% endif %}>Flyer</a></li>
          <li><a href="/logs/"{% if page.url == "/logs/" %} class="active"{% endif %}>Logs</a></li>
        </ul>
      </nav>
    </div>
  </header>

  <main>
    {{ content | safe }}
  </main>

  <!-- Floating Footer Banner -->
  <div class="floating-footer" id="floatingFooter">
    <button class="close-btn" onclick="closeFooter()" aria-label="Close banner">&times;</button>
    <a href="https://moreradio.club/" target="_blank" rel="noopener">
      <img src="/assets/images/more-radio-banner.svg" alt="More Radio - More Radio creates More Fun attracts More People enables More Radio">
    </a>
  </div>

  <script>
    function closeFooter() {
      document.getElementById('floatingFooter').style.display = 'none';
      sessionStorage.setItem('footerClosed', 'true');
    }

    // Check if user closed the footer this session
    if (sessionStorage.getItem('footerClosed') === 'true') {
      document.getElementById('floatingFooter').style.display = 'none';
    }

    function closeTopBanner() {
      document.getElementById('topBanner').style.display = 'none';
      document.body.classList.add('top-banner-closed');
      sessionStorage.setItem('topBannerClosed', 'true');
    }

    // Check if user closed the top banner this session
    if (sessionStorage.getItem('topBannerClosed') === 'true') {
      document.getElementById('topBanner').style.display = 'none';
      document.body.classList.add('top-banner-closed');
    }

    // Mobile menu toggle
    const menuToggle = document.getElementById('menuToggle');
    const siteNav = document.getElementById('siteNav');

    menuToggle.addEventListener('click', function() {
      const isExpanded = menuToggle.getAttribute('aria-expanded') === 'true';
      menuToggle.setAttribute('aria-expanded', !isExpanded);
      siteNav.classList.toggle('nav-open');
      menuToggle.classList.toggle('menu-open');
    });
  </script>
</body>
</html>
